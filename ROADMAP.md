# 할 일 — **내가 들고 굴린다**

## 건드리면 안 되는 것 — **병수님이 정한 것**

- **유닛은 벙커 안에 있다. 판에 꺼내 그리지 않는다.**
  2026-08-07 에 "관전이 얇다"를 풀겠다고 유닛을 벙커 둘레에 그렸다가 되돌렸다(`c393623`).
  병수님: *"아니 유닛 밖으로 빼지말라니까 자꾸 빼네,, 그거 별로라고."*
  **관전이 얇다는 문제는 다른 방법으로 푼다** — 탄·이펙트·벙커 연출 쪽으로.
- 한 종류는 한 자리만 · 보유도 종류당 한 기
- 화면에 나가는 말은 어미 없는 짧은 명사구(2026-08-06 에 정함)

병수님이 다음 할 일을 정해 주지 않아도 이 목록을 위에서부터 소화한다. 갈림길은 묻지 않고
정한다. 매 작업 끝에 **"병수님이면 뭘 지적할까"**를 스스로 돌리고, 나온 것을 여기에 넣는다.

(2026-08-07 병수님: "이런거 다 내가 하나하나 지적해서 하려니까 너무 지친다.")

## 지금 하는 것


- [ ] **보스를 사건으로 — 남은 것: 고유 스프라이트** — 이름 있는 놈 셋이 아직 다 `boss.png`
      한 장이다(크기·색조·테·이름표로만 갈라 놨다). PixelLab 로 셋의 몸을 따로 굽고,
      능력도 그때 고유한 것으로 하나씩(지금은 있는 다섯 중 둘을 섞은 것)
- [ ] **걷기 프레임 정식** — 지금은 한 장으로 낸 대역(바운스·반전·접지 그림자).
      PixelLab `animate_character` 로 종류별 걷기 4~6프레임

## 그다음

- [ ] **유닛 성장 갈래** — 같은 저격수라도 「관통 특화 / 한 방 특화」. 12종이 24갈래가 되고
      환생할 때마다 다른 길을 탄다
- [ ] **저장 이동** — 폰↔PC 내보내기/불러오기 (지금은 기기별 별개)
- [ ] **2번째 지역** — 배경·적 세트가 다른 맵. 제일 크고 제일 비싸다

## 자체 검수에서 나온 것

- [ ] **다섯 판 앞서 알아도 그때 무엇을 해야 하는지는 안 적힌다** — 「3R 뒤 강철 파괴자」가
      뜨지만 그놈이 무엇에 약한지는 코앞에 와야 나온다(유리 칩은 다음 라운드 것이다).
      칩을 누르면 그놈의 약점·능력이 뜨는 쪽(자리를 안 먹는다)
- [ ] **넷째 이름 있는 놈** — 바퀴마다 험해지긴 하나 얼굴은 여전히 셋이다. 몸이 다른
      넷째(그리고 다섯째)를 더하면 한 바퀴가 길어져 재탕 간격 자체가 벌어진다.
      PixelLab 스프라이트가 필요하니 「보스 고유 스프라이트」와 같이 굽는다

- [ ] **머리줄 자원은 눌러도 정확한 수가 안 뜬다** — 그 자리는 버튼이라 누르면 상점이 열린다
      (그게 맞다). 다만 판 도중 제일 오래 보는 수가 그 하나라 「128만」의 원래 수를 보려면
      상점을 열어야 한다. 길게 누르기로 가르든지, 상점 첫 줄에 크게 적든지
- [ ] **점선 밑줄이 무슨 뜻인지 화면이 말한 적이 없다** — 「눌러서 그 판」처럼 뜻풀이 줄에
      한마디가 필요하다. 다만 밑줄은 이미 320px 에서 세 줄이라 자리부터 비워야 한다
- [ ] 유닛이 판에 안 보인다(벙커 안) — 누가 쏘는지가 안 읽혀 관전이 얇다
- [ ] 「난장」(56R) 아래 라운드대는 테마가 촘촘한데 60R 위로는 다시 순환뿐이다
- [ ] **날 경계 선이 무슨 선인지 안 적혀 있다** — 막대를 누르면 "· 어제"가 뜨지만 선 자체의
      뜻풀이는 없다. 뜻풀이 줄은 320px 에서 한 줄이 한계라(histlegend) 한 칸을 더 못 넣었다.
      선 위에 날짜 눈금을 얹거나, 뜻풀이 줄을 두 단으로 여는 쪽
- [ ] **밑줄이 320px 에서 세 줄이다** — 「첫 10판(넉~석 달 전) 평균」을 붙이며 딱 세 줄까지
      찼다. 한 항목만 더 붙으면 넉 줄이 된다. 「한 판에 평균 N기 처치」를 위 네 칸 쪽으로
      옮기든지 해서 자리를 비워 둘 것

## 끝난 것

- [x] **뽑기의 희귀도** — 흔함·드묾·귀함. 무게는 「어느 새 얼굴이 먼저 오느냐」에만 건다
      (전체에 걸면 넓이가 깎여 counteraudit 이 -2.5R 로 뒤집혔다). 겹침은 조각 1~3

- [x] **새 얼굴이 나오는 자리를 곡선에 맞춘다** — 25·32·40·48·56 → 12·18·26·34·42.
      사람이 실제로 노는 R5~R20 에서 다섯 종만 돌던 것이 반복의 나머지 절반이었다

- [x] **예고가 몇 판 앞서 이름을 댄다** (`dab4525`) — 25R 바로 앞에서야 이름이 떠 갈아 끼울
      시간이 없었다. 다섯 판 안이면 「3R 뒤 강철 파괴자」 점선 칩(머리줄·배치 화면 둘 다).
      검증 tools/namedsoon.mjs
- [x] **한 바퀴 돌 때마다 더 험한 놈** — 25·50·75 다음이 다시 강철 파괴자였다. 이름 있는
      놈이 셋뿐이라 100R 부터는 순환일 뿐 사건이 아니었다. 바퀴마다 몸 +60%, 능력을 하나씩
      더(둘→셋→넷, 다섯이 상한), 이름 앞에 그 바퀴의 수식(거듭난·굶주린·심연의·잿빛,
      그 위는 「6대」)을 붙였다. 예고 칩과 배치 화면이 같은 `namedBoss` 를 보므로 긴 이름이
      그대로 예고에 뜬다 — 갈아 끼울 마음은 판에서 만나기 전에 들어야 한다.
      `tools/namedboss.mjs` 에 두 바퀴째 라운드(100·150·175)를 더해 이름표가 새는지와
      능력이 실제로 늘었는지를 같이 잰다.

- [x] **예고가 이름 있는 놈 이름을 댄다** — 보스 라운드 칩은 어느 놈이든 늘 같은 boss.png
      한 장이라 「큰 놈」까지밖에 못 읽었다. 25R 짜리는 **준비할 시간을 주는 것**이 요점인데
      배너가 뜰 때는 이미 판에 와 있다. 머리줄 칩과 배치 화면 첫 줄에 그놈 이름을 적고,
      판 위에서 쓰는 그 색(`ring`·`hue`)을 그대로 얹었다. 좁은 폰에서 종류 이름은 접히지만
      (font-size:0) 이 칩은 안 접는다 — 스물다섯에 한 번뿐이라 접으면 뜻이 없다.
      자: `node tools/namedwarn.mjs` (320/430px · 25·50·75R · 테 색 셋이 다른지 ·
      이름 없는 보스 라운드(20R)엔 안 뜨는지). funtest 10판 ✓ 다섯 줄 그대로

- [x] **상성 배수가 등급을 탄다** — 1등급 ×2 → 5등급 ×6. 다 키운 뒤에도 무엇을 내보낼지가
      남는다. 떼·분열체의 「범위에 약함」은 말만 있고 수치가 없어서 같이 넣음

- [x] **이름 있는 놈이 판에서 안 읽혔다** — 스물다섯 라운드에 한 번뿐인데 배너 한 줄이
      지나가면 보통 보스와 똑같은 붉은 덩어리였다(같은 그림·같은 76px·같은 테).
      스프라이트가 아직 한 장이라 **크기·색조·테·이름표**로 가른다: 98px · 놈마다 다른 색
      (강철=쇠, 역병=초록, 장막=자주) · 숨 쉬는 테 · 몸에 붙은 이름표.
      `cssText` 를 매 프레임 통째로 갈아치우므로 색은 거기 같이 실었다(따로 걸면 날아간다).
      자: `node tools/namedboss.mjs` (25·50·75R · 크기 · 테 색 셋이 다른지 · 이름표가
      체력바를 안 덮는지). funtest 10판 ✓ 다섯 줄 그대로

- [x] **줄여 적은 수를 폰에서 읽는다 — 손잡이 하나로** — 「128만」의 원래 수가 `title` 뿐이라
      마우스 없는 화면에선 영영 못 읽었다. 붙은 자리가 여섯이라 자리마다 달면 다음에 하나 더
      붙일 때 또 빼먹는다 — 문서에 딱 하나 걸고(`wireExact`), 표시는 `data-ex` 를 단 것이
      알아서 뜬다(`setNum`·`exNum` 이 달아 준다). 누른 자리 위에 방울로 2.2초, 화면 밖으로
      안 나가게 좌우를 물린다. **버튼 안의 수는 안 건드린다** — 머리줄 자원은 눌리면 상점을
      열어야 한다. 자: `node tools/exactnum.mjs` (320/390px · 12자리 · 딴 데 누르면 사라짐까지)
- [x] **줄여 적기가 기록 화면에만 있다** — 쟀더니 **폭은 안 밀렸다**: 자원에 아홉 자리
      (987,654,321)를 넣어도 머리통은 40px 그대로고 상점 밑줄도 두 줄 그대로다(320~430px).
      머리줄은 `flex`+`wrap` 이라 넉 장처럼 옆칸을 빼앗는 구조가 아니었다.
      진짜 결함은 폭이 아니라 **읽기**였다 — 자원은 쉼표조차 없이 「1284000」으로 나갔다.
      자원 셋(머리줄·상점 둘)과 전투력 둘(상점·배치)에 `shortNum` + 정확한 수 title
      (`setNum` 으로 둘을 묶음). 제일 비싼 것이 세 자리라 줄여 적기가 도는 만 위에서
      살지 말지가 헷갈릴 일은 없다. 자: `node tools/numwidth.mjs`
- [x] **기록 넉 장의 폭이 안 고르다** — `min-width:auto` 라 「1,284,000」이 제 칸을 넓히고
      옆칸을 빼앗았다(320px 에서 96 / 39 / 65 / 55px). `min-width:0` 으로 못 박고 큰 수는
      「128만」·「1.2억」으로 줄여 적는다(`shortNum`, 정확한 수는 title). 넉 폭 다 60px.
      자: `node tools/statlabel.mjs` — 이제 기울면 ✗ 로 센다
- [x] **이름표가 갈라져 기록 넉 장이 들떴다** — 「최고 라운드」가 320~360px 에서 두 줄.
      한 장만 커지는 게 아니라 grid 줄 전체가 53→65px 로 뜬다. 범인은 letter-spacing:1px
      (10px 한글에선 읽히지도 않으면서 폭만 6px). 자간을 지우고 nowrap 으로 못 박았다.
      320px 에선 「최근 평균」도 같이 갈라지고 있었다. 자: `node tools/statlabel.mjs`
- [x] **첫 사람의 기록 화면이 안 비어 있다** — 한 판도 안 끝냈으면 오르는 예시 그래프 열넉 칸을
      흐리게 깔고 「예시」를 겹쳐 둔다. 안 눌리고, 첫 판이 끝나면 통째로 사라진다.
      자: `node tools/histempty.mjs`
- [x] **밑줄이 첫 판이 언제였는지 말한다** — 「첫 10판(넉~석 달 전) 평균 8.3라운드」.
      시기가 막대 설명 안에만 있어 눌러야 보이던 것을 안 눌러도 읽히게.
      시각 없던 옛 세이브엔 괄호가 안 붙는다. 자: `node tools/histfirst.mjs`
- [x] **접힌 묶음이 언제 것인지 말한다** — epochs 가 t0·t1 을 들어 「넉~석 달 전 10판 평균」.
      시각 없던 옛 세이브는 그대로 「옛 」. 자: `node tools/histepoch.mjs`
      (겸사겸사 「1달 전」 → 「한 달 전」 우리말 셈씨로)
- [x] **날 경계 선** — 날이 바뀌는 자리에 얇은 선. 안 눌러도 "어제 열 판" 묶음이 읽힌다.
      자: `node tools/histday.mjs` (무리 넷 → 선 셋 · 틈 안 · 시각 없는 옛 세이브엔 0줄)
- [x] 기록에 **날짜**가 붙는다 — 판마다 끝낸 시각을 들고, 막대를 누르면 "22라운드 · 오늘",
      밑줄에 "굴린 지 36일째 · 오늘 2판". 시각 없던 옛 세이브는 이 줄이 안 뜬다
- [x] 옛 묶음이 **멀수록 흐리게** — 넷이 다 열 판이라 폭으로는 못 가른다. 바램이 곧 거리
      (0.56→1.0). 뜻풀이 줄은 320px 에서 두 줄로 접히던 것을 한 줄로 (`tools/histlegend.mjs`)
- [x] 옛 판 막대를 사선 무늬와 밝은 윗선으로 가른다 — 색만으로는 폰에서 구별 불가였다
- [x] 접힌 막대를 폰에서도 읽는다 — 뜻풀이 줄 + 눌러서 보기 (`759fec7`)
- [x] 자동 채움을 빈 자리만으로 — 배치를 사람에게 돌려줌 (`d44020b`)
- [x] 상성을 실제 수치로 + 웨이브 테마 (`d44020b`)
- [x] 판 도중 전리품 — 파밍의 리듬 (`36d6c30`)
- [x] 전투력 표시 — 성장이 보이게 (`36d6c30`)
- [x] 적이 걷는다 — 걸음·접지·방향 (`9394b21`)
- [x] 적 넷을 뒤에 연다 — 분열·치유·포격·자폭 (`605d0de`)
- [x] 적 도감 + 이름 있는 보스 (`32c371b`)
- [x] 포격체 사거리 밖 표식 · 자폭체 심지 경고 (`self-review`)
- [x] 기록 화면 — 누적 처치·판 수·최근 24판 이력 (`7e3a49f`)
- [x] 초반 난이도 — 첫 판이 7R 에서 끝나고 판마다 눈에 띄게 는다 (병수님 요청)
- [x] 정산에 "최근 여덟 판 중 두 번째" · 탭 닫을 때 처치 저장 (`e460bd1`)
- [x] 이력이 24판에서 안 잘린다 — 밀려난 판은 열 판 평균 한 칸으로 접어 그래프 왼쪽에 남는다
- [x] funtest ✗ 는 게임이 아니라 **자의 표본 문제**였다 — 뒤 절반 판정으로 고침
- [x] 첫 판은 못 깬다 — 5R 관문에서 막히고 강화해야 넘는다 (`75e909d`)
- [x] 「추천 배치」가 역할 자리를 떼어 놓는다 — 무작위 봇에 지던 버튼을 고침 (`75e909d`)
